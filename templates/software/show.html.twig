<!DOCTYPE html>

<title>Software</title>

{% block body %}
    <h1>Software</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ software.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ software.name }}</td>
            </tr>
            <tr>
                <th>Constructor</th>
                <td>{{ software.constructor }}</td>
            </tr>
            <tr>
                <th>Category</th>
                <td>{{ software.category }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('software_index') }}">back to list</a>

    <a href="{{ path('software_edit', {'id': software.id}) }}">edit</a>

    {{ include('software/_delete_form.html.twig') }}
{% endblock %}
