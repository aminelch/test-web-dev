{% for type, messages in app.flashes(['success', 'warning','danger', 'error', 'info']) %}
    {% for message in messages %}
        <div class="alert alert-{{ type }}" role="alert">
            {{ message }}
        </div>
    {% endfor %}
{% endfor %}


